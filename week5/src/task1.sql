CREATE TABLE cqupt_student(
                              studentid VARCHAR(10),
                              NAME VARCHAR(20),
                              sex VARCHAR(2),
                              age INTEGER,
                              Fee DECIMAL(10,2),
                              address VARCHAR(50),
                              memo VARCHAR(300)
);